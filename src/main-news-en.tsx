import React from "react";
import ReactDOM from "react-dom/client";
import { NewsPageEn } from "./pages/NewsPageEn";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <NewsPageEn />
  </React.StrictMode>,
);
